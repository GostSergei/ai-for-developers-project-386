# Базовый URL API. В dev по умолчанию — Prism mock (npm run mock в корне).
# Для работы с отдельно запущенным бэкендом укажите его адрес, например:
# VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=http://localhost:4010
